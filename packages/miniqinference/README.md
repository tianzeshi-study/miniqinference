# miniqinference

Meta package for `miniqinference` components.

## Installation

```bash
# Install the CLI
pip install miniqinference[cli]

# Install the Server
pip install miniqinference[server]

# Install all components
pip install miniqinference[all]
```
